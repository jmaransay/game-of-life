
language: java

jdk:
- oraclejdk8
- oraclejdk9
- openjdk8

matrix:
  allow_failures:
  - jdk: oraclejdk9
  
install:
 - sudo apt-get install nmap
 - sudo add-apt-repository ppa:cpick/hub
 - sudo apt-get install hub
